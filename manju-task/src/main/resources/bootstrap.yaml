spring:
  application:
    name: task-service #服务名
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.100.128:8848 #nacos地址
      config:
        file-extension: yaml
  config:
    import: #导入nacos配置
      - nacos:shared-jdbc.yaml
      - nacos:shared-log.yaml
#      - nacos:shared-redis.yaml
      - nacos:ai-service.yaml
      - nacos:aigc-service.yaml
      - nacos:prompt.config.yaml