server:
  port: 8080
spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      server-addr: 192.168.100.128:8848
    gateway:
      routes:
        - id: user
          uri: lb://user-service
          predicates:
            - Path=/auth/**
  profiles:
    active: dev
